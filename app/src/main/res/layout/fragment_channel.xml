<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#eceeee"
    android:paddingTop="20dp">

    <TextView
        android:id="@+id/textView_totalChannel"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:gravity="center_vertical"
        android:layout_marginLeft="20dp"
        android:text="@string/bookmark_channel"
        android:textColor="#000000" />

    <View
        android:layout_alignParentBottom="true"
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="50dp" />

    <GridView
        android:id="@+id/gridView_channelList"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/view"
        android:layout_below="@id/textView_totalChannel"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="15dp"
        android:columnWidth="110dp"
        android:fadingEdge="none"
        android:listSelector="#00000000"
        android:numColumns="3"
        android:scrollingCache="false"
        android:stretchMode="spacingWidthUniform"
        android:verticalSpacing="7.5dp"/>


</RelativeLayout>